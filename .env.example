# Typesense Configuration
# For local development, run: docker run -p 8108:8108 -v/tmp/typesense-data:/data typesense/typesense:latest --data-dir /data --api-key=xyz --enable-cors

# Public variables (exposed to client)
PUBLIC_TYPESENSE_HOST=localhost
PUBLIC_TYPESENSE_PORT=8108
PUBLIC_TYPESENSE_PROTOCOL=http

# Private variables (server-only)
TYPESENSE_API_KEY=xyz

# Production Typesense Cloud example:
# PUBLIC_TYPESENSE_HOST=xxx.a1.typesense.net
# PUBLIC_TYPESENSE_PORT=443
# PUBLIC_TYPESENSE_PROTOCOL=https
# TYPESENSE_API_KEY=your_admin_api_key
